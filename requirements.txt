# Core Microsoft 365 Agents SDK (used in Phase 1–7)
microsoft-agents-hosting-aiohttp>=1.0.0
microsoft-agents-hosting-core>=1.0.0
microsoft-agents-activity>=1.0.0

# Authentication (Phase 7+)
microsoft-agents-authentication-msal>=1.0.0

# Teams channel extension (Phase 7)
microsoft-agents-hosting-teams>=1.0.0

# Persistent storage (Phase 3 advanced)
microsoft-agents-storage-blob>=1.0.0
microsoft-agents-storage-cosmos>=1.0.0

# LLM integration (Phase 5+)
openai>=1.30.0
azure-identity>=1.17.0

# RAG (Phase 6)
azure-search-documents>=11.4.0

# Utilities
python-dotenv>=1.0.0
aiohttp>=3.9.0
pydantic>=2.5.0

# === Agent 365 enterprise SDK (Phase 8) ===
# Install separately with pre-releases allowed when you reach Phase 8:
#   pip install microsoft-agents-a365-runtime microsoft-agents-a365-observability-core \
#               microsoft-agents-a365-observability-hosting microsoft-agents-a365-notifications \
#               microsoft-agents-a365-tooling --pre

# === Testing & dev tools ===
pytest>=8.0.0
pytest-asyncio>=0.23.0
black>=24.0.0
flake8>=7.0.0
